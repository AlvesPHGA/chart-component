import styled from 'styled-components';

export const Footer = styled.footer`
   padding-top: ${20 / 16}rem;
   text-align: center;
   color: ${(props) => props.theme.colors.primary_softRed};
   opacity: 0.5;
`;
